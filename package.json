{
  "name": "ne503-ci",
  "private": true,
  "description": "Root package for NE503 AIPC platform repository tooling.",
  "scripts": {
    "commitlint": "commitlint --from=origin/main --to=HEAD"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0"
  }
}
