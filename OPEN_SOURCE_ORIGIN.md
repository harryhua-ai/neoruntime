# Open Source Origin

This repository was prepared from the internal NE503 AIPC workspace using:

internal open-source split tooling.

Target upstream repository: `camthink-ai/ne503-aipc`

Before publishing, run the repository-specific checks in `docs/OPEN_SOURCE_SPLIT.md`
from the source workspace and review this snapshot for secrets, proprietary
vendor assets, generated binaries, model files, and customer data.
